# Databricks SQL Warehouse (server-side only; never use the VITE_ prefix)
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_HTTP_PATH=
DATABRICKS_WAREHOUSE_ID=
DATABRICKS_CATALOG=workspace
DATABRICKS_SCHEMA=fplgeek

# Optional: used by protected data-refresh endpoints when enabled.
UPDATE_ADMIN_SECRET=